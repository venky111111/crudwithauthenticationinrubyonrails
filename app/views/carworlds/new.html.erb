<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New CarDetails</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      background-image: url('https://images.unsplash.com/photo-1517994112540-009c47ea476b?q=80&w=1000&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxleHBsb3JlLWZlZWR8Mnx8fGVufDB8fHx8fA%3D%3D');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat; 
      height: 100vh;
    }

    header {
      background-color: #333;
      color: white;
      padding: 20px;
      text-align: center;
    }

    nav {
      background-color: #f2f2f2;
      padding: 10px;
      text-align: center;
    }

    nav a {
      text-decoration: none;
      color: green;
      padding: 10px;
      margin-right: 10px;
    }

    .container-form {
      max-width: 400px;
      margin: 80px auto;
      padding: 20px;
      background-color: white ;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
    }

    h3 {
      text-align: center;
      
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: bold;
    }

    input {
      width: 100%;
      padding: 10px;
      margin-bottom: 16px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }

    input[type="submit"],.back-button {
      background-color: #2196F3;
      color: white;
      border: none;
      border-radius: 4px;
      padding: 12px 24px;
      cursor: pointer;
      font-size: 16px;
      margin-top: 10px;
      width: 100%;
      text-decoration:none;
    }

    footer {
      background-color: #333;
      color: white;
      padding: 10px;
      text-align: center;
      position: fixed;
      bottom: 0;
      width: 100%;
    }
  </style>
</head>
<body>

  <header>
    <h1>Create a New CarDetails</h1>
  </header>
  <nav>
    <a href="#">Home</a>
    <a href="#">About</a>
    <a href="#">Contact</a>
  </nav>

  <%= form_with model: @carworld, class: "container-form" do |form| %>
    <div class="container">
      <h3>Create a New CarDetails</h3>
      <label for="brand">Brand:</label>
      <%= form.text_field :brand, id: "brand", placeholder: "Enter brand", required: true %>

      <label for="model">Model:</label>
      <%= form.text_field :model, id: "model", placeholder: "Enter model", required: true %>

      <%= form.submit "Create CarDetails", class: "submit-button" %>
      <%= link_to "Back to Cars", carworlds_path, method: :get, class: 'back-button' %>
      </form>
    </div>
  <% end %>

  

  <footer>
    &copy; 2023 Cars Data App
  </footer>

</body>
</html>
